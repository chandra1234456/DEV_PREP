name: Dummy Workflow

on:
  push:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Say Hello
        run: echo "Hello! Workflow is running successfully 🚀"

      - name: List files
        run: ls -R

      - name: Done
        run: echo "No deployment, just testing CI/CD"